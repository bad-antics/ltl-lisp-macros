// examples/demo.ltl — runnable demo for ltl_lisp_macros

import std::io
use ltl_lisp_macros as lib

fn main() {
    io::println("ltl_lisp_macros demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}